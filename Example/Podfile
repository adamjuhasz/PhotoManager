source 'https://github.com/CocoaPods/Specs.git'

target 'PhotoManager', :exclusive => true do
  pod "PhotoManager", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "PhotoManager", :path => "../"

  pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
